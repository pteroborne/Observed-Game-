The Borrowed View — verification ledger
Date: 2026-09-15

Workspace checks
  cargo fmt --all: passed.
  cargo dev-clippy: passed, no warnings (13.80 s final run).
  cargo dev-test -- --skip tests::a_tower_climbs_with_another_standing_on_it:
    exit 0; 2,103 passed, 0 failed, 37 ignored, 1 filtered out.
  The filtered stacked-tower test passed in the preceding unfiltered run.
  Combined coverage: 2,104 passed, 37 existing ignored tests.

The initial unfiltered run completed the long stacked-tower test but failed
the corpus's pinned maximum-hull tile name: borrowed_fork now ties the existing
36-hull maximum and sorts first. The name expectation was corrected; the hull
budget was not raised. The final run repeated all other tests after the last
material and roof-practical changes. The tower geometry/controller did not
change between these runs.

Relevant coverage
  Six sources regenerate byte-for-byte and meet production geometry demands.
  Production controller traverses all 22 ordered doorway pairs, without jumps
  or falls, using routes around the screens.
  Small-capsule probes distinguish rail gaps from solid rails; a player-sized
  capsule cannot fit inside a screen gap.
  Eleven-tile layout resolves, matches adjacent ports, remains connected and
  survives three resets without accumulating collision state.
  Shared paper/timber material contrast and opaque lattice pixels are pinned.
  Roof-section filtering removes offset practicals with their parent module.
  Both spectator candidate-selection digests pass with the expanded catalogue.

Source seam audit
  cargo run -p observed_authoring --bin tilec -- audit-seams: GREEN PASSED.
  413 strict sources, 0 legacy, 1,289 declared ports, 0 unresolved.
  549,676 valid seamless boundaries; 0 height mismatches.
  Vertical compatibility sources without compiled interface contracts are
  reported but not compared by this audit. Separate controller tests cover
  movement; this composition has no vertical connections.

Catalogue hash
  dea61d2a4834e0dbcbe697ac01f273a926937aaff8577383ce6060fdfe2208bb
Composition profile hash (unchanged)
  5c1bc69db058d4e3332e755326548f887d46f215d81fdeb454591cd9c2c0104e
Folded simulation hash
  6ac9c845670f2d5d675d8f6361631af48b110bbb696e0dfbb6f07123e36bfadd

Evidence
  Concept generated before source authoring; exact prompt preserved.
  Six 1500x900 captures produced by hex_tile_lab from the checked-in scripts:
  hero, clay, plan, layered, screen and loop. All inspected visually.
  Plan framing was rotated after the test run to include the full composition;
  its placement list and geometry were unchanged.
  Screen CAD SVG generated by tilec; PNG rasterized with rsvg-convert.
  First-person images are stationary facility-lighting captures, not recorded
  playthroughs. Inspection cutaways use photographic fill and exposure.
  Concept-to-model limitations are recorded in README.md.

Session logs (temporary, not portable evidence)
  /tmp/borrowed-clippy-final.log
  /tmp/borrowed-tests-final.log
  /tmp/borrowed-tests.log
  /tmp/borrowed-seams.log
