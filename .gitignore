/target
# Parallel worktrees set their own CARGO_TARGET_DIR to avoid corrupting the
# shared one (see docs/arc_t/README.md §2a). Those live beside the worktree and
# must never be committed — `/target` above is root-anchored and would not catch
# them, and workers are told to `git add -A`.
target-*/
/dist/
/scratch/
/saves/
/game/saves/
*.pdb
.claude/
docs/evidence/bot_pov/bot_pov_*.png
docs/evidence/bot_pov/palette.png
docs/evidence/hex_wfc/relayout/frame_*.png
docs/evidence/debug_coercion*/
docs/evidence/visual_audit_camera_rooms/
docs/evidence/visual_audit_topdown/
we-need-to-come-tidy-emerson.md

# Python bytecode from tools/tileforge.py
__pycache__/
