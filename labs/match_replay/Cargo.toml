[package]
name = "match_replay"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = ["2d", "png"] }
observed_core = { path = "../../crates/observed_core" }
observation_lab = { path = "../observation_lab" }
competition_lab = { path = "../competition_lab" }
director_lab = { path = "../director_lab" }
competitive_facility = { path = "../competitive_facility" }
