[package]
name = "session_lab"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = ["2d", "png"] }
observed_core = { path = "../../crates/observed_core" }
# The session/matchmaking model now lives in the production crate; this lab projects it.
observed_progression = { path = "../../crates/observed_progression" }
